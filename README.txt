This is a source distribution of BValid, a Java library for XML validation.

To build, make sure ant is in your path and type:

ant dist

Then, to run the command-line validation utility, cd to dist and type:

bvalid --help

For more information, see http://bvalid.sf.net/
